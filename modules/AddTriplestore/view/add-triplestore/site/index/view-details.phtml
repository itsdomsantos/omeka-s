<?php
$this->headTitle($resource->displayTitle() . ' | Archaeological Data');
$this->headLink()->appendStylesheet($this->assetUrl('css/addTriplestore.css', 'AddTriplestore'));

$resourceTitle = $resource->displayTitle();
$resourceDescription = $resource->displayDescription();
$resourceType = ($resourceType === 'item_set') ? 'Excavation' : 'Artifact';
?>

<div class="resource-details">
    <div class="resource-header">
        <div class="breadcrumbs">
            <a href="<?php echo $this->url('site', ['site-slug' => $site->slug()]); ?>">Home</a> &gt;
            <a href="<?php echo $this->url('site/add-triplestore/search', ['site-slug' => $site->slug()]); ?>">Search</a> &gt;
            <?php echo $this->escapeHtml($resourceTitle); ?>
        </div>
        
        <h1><?php echo $this->escapeHtml($resourceTitle); ?></h1>
        
        <?php if ($resourceDescription): ?>
            <div class="resource-description">
                <?php echo $resourceDescription; ?>
            </div>
        <?php endif; ?>
    </div>
    
    <div class="resource-content">
        <div class="resource-main">
            <?php if ($resourceType === 'item_set'): ?>
    <div class="excavation-details">
        <div class="metadata-section">
            <h2>Excavation Details</h2>
            <table class="metadata-table">
                <?php foreach ($properties as $property): ?>
                    <tr>
                        <th><?php echo $this->escapeHtml($property['label']); ?></th>
                        <td>
                            <?php foreach ($property['values'] as $value): ?>
                                <div class="value">
                                    <?php echo $value->type() === 'uri' ? '<a href="' . $this->escapeHtml($value->uri()) . '">' . $this->escapeHtml($value->value()) . '</a>' : $this->escapeHtml($value->value()); ?>
                                </div>
                            <?php endforeach; ?>
                        </td>
                    </tr>
                <?php endforeach; ?>
            </table>
        </div>
        
        <script>
            console.log('=== EXCAVATION PAGE DEBUG ===');
            console.log('Resource Type:', <?php echo json_encode($resourceType); ?>);
            console.log('Related Items Count:', <?php echo count($relatedItems ?? []); ?>);
            console.log('Related Items Empty?', <?php echo empty($relatedItems) ? 'true' : 'false'; ?>);
            console.log('Related Items Array:', <?php echo json_encode($relatedItems ?? []); ?>);
            <?php if (!empty($relatedItems)): ?>
                console.log('First item title:', <?php echo json_encode($relatedItems[0]->displayTitle()); ?>);
            <?php endif; ?>
        </script>
        
        <?php if (!empty($relatedItems)): ?>
            <div class="related-items">
                <h2>Artifacts in this Excavation (<?php echo count($relatedItems); ?>)</h2>
                <div class="result-cards">
                    <?php foreach ($relatedItems as $item): ?>
                        <div class="result-card">
                            <?php if ($item->thumbnail()): ?>
                                <div class="thumbnail">
                                    <img src="<?php echo $this->escapeHtml($item->thumbnail()->assetUrl()); ?>" alt="<?php echo $this->escapeHtml($item->displayTitle()); ?>">
                                </div>
                            <?php endif; ?>
                            <h4><?php echo $this->escapeHtml($item->displayTitle()); ?></h4>
                            <?php if ($item->displayDescription()): ?>
                                <div class="description"><?php echo $this->escapeHtml(substr($item->displayDescription(), 0, 100)); ?>...</div>
                            <?php endif; ?>
                            <div class="actions">
                                <a href="<?php echo $this->url('site/add-triplestore/view-details', ['site-slug' => $site->slug()], ['query' => ['id' => $item->id(), 'type' => 'item']]); ?>" class="details-button">View Details</a>
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>
            </div>
        <?php else: ?>
            <div class="no-related-items">
                <p>No artifacts found in this excavation.</p>
                <a href="<?php echo $this->url('site/add-triplestore/upload', ['site-slug' => $site->slug()], ['query' => ['item_set_id' => $itemSetIdForLink, 'upload_type' => 'arrowhead', 'mode' => 'form']]); ?>" class="add-button">Add Artifacts to this Excavation</a>
            </div>
        <?php endif; ?>
                </div>
            <?php else: ?>
                <div class="artifact-details">
                    <div class="artifact-media">
                        <?php if ($resourceType === 'Artifact'): ?>
        <?php $mediaList = $resource->media(); ?>
        <?php if (count($mediaList) > 0): ?>
            <div class="media-carousel" id="media-carousel">
                <?php foreach ($mediaList as $i => $media): ?>
                    <div class="carousel-slide" style="display:<?php echo $i === 0 ? 'block' : 'none'; ?>">
                        <a href="<?php echo $media->originalUrl(); ?>" target="_blank">
                            <img src="<?php echo $media->thumbnailUrl('large'); ?>" alt="<?php echo $this->escapeHtml($media->displayTitle()); ?>">
                        </a>
                        <?php if ($media->displayTitle()): ?>
                            <div class="media-caption"><?php echo $this->escapeHtml($media->displayTitle()); ?></div>
                        <?php endif; ?>
                    </div>
                <?php endforeach; ?>
                <button class="carousel-nav prev" id="carousel-prev">&#10094;</button>
                <button class="carousel-nav next" id="carousel-next">&#10095;</button>
                <div class="carousel-indicators">
                    <?php foreach ($mediaList as $i => $media): ?>
                        <span class="carousel-dot<?php echo $i === 0 ? ' active' : ''; ?>" data-index="<?php echo $i; ?>"></span>
                    <?php endforeach; ?>
                </div>
            </div>
        <?php else: ?>
            <div class="no-media-message">
                <em>No images available for this artifact.</em>
            </div>
        <?php endif; ?>
    <?php endif; ?>
                    </div>
                    
                    <div class="artifact-context">
                        <h3>Archaeological Context</h3>
                        <div class="context-info">
                            <?php 
                            $itemSets = ($resourceType === 'Artifact') ? $resource->itemSets() : [];
                            if (count($itemSets) > 0): 
                            ?>
                                <div class="excavation-info">
                                    <span class="label">Excavation:</span>
                                    <?php foreach ($itemSets as $index => $itemSet): ?>
                                        <a href="<?php echo $this->url('site/add-triplestore/view-details', ['site-slug' => $site->slug()], ['query' => ['id' => $itemSet->id(), 'type' => 'item_set']]); ?>">
                                            <?php echo $this->escapeHtml($itemSet->displayTitle()); ?>
                                        </a><?php if ($index < count($itemSets) - 1) echo ', '; ?>
                                    <?php endforeach; ?>
                                </div>
                            <?php endif; ?>
                            
                            <?php 
                            $contextProperties = [
                                'dcterms:spatial' => 'Location',
                                'dcterms:temporal' => 'Time Period',
                                'ah:chippingLocation' => 'Chipping Location',
                            ];
                            
                            foreach ($properties as $property): 
                                if (isset($contextProperties[$property['term']])): 
                            ?>
                                <div class="context-property">
                                    <span class="label"><?php echo $this->escapeHtml($contextProperties[$property['term']]); ?>:</span>
                                    <?php foreach ($property['values'] as $index => $value): ?>
                                        <span><?php echo $this->escapeHtml($value->value()); ?></span><?php if ($index < count($property['values']) - 1) echo ', '; ?>
                                    <?php endforeach; ?>
                                </div>
                            <?php 
                                endif;
                            endforeach; 
                            ?>
                        </div>
                    </div>
                    
                    <div class="metadata-section">
                        <h2>Artifact Metadata</h2>
                        <table class="metadata-table">
                            <?php foreach ($properties as $property): ?>
                                <tr>
                                    <th><?php echo $this->escapeHtml($property['label']); ?></th>
                                    <td>
                                        <?php foreach ($property['values'] as $value): ?>
                                            <div class="value">
                                                <?php echo $this->escapeHtml($value->value()); ?>
                                            </div>
                                        <?php endforeach; ?>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        </table>
                    </div>
                </div>
            <?php endif; ?>
        </div>
        
        <div class="resource-sidebar">
            <div class="actions-panel">
                <h3>Actions</h3>
                <ul class="action-list">
                    <li>
                        <a href="<?php echo $this->url('site/add-triplestore/search', ['site-slug' => $site->slug()]); ?>" class="action-link">
                            <span class="icon">🔍</span> Back to Search
                        </a>
                    </li>
                    
                    <?php if ($resourceType === 'Excavation'): ?>
                        <li>
                            <a href="<?php echo $this->url('site/add-triplestore/upload', ['site-slug' => $site->slug()], ['query' => ['item_set_id' => $itemSetIdForLink, 'upload_type' => 'arrowhead', 'mode' => 'form']]); ?>" class="action-link">
                                <span class="icon">➕</span> Add Artifacts to this Excavation
                            </a>
                        </li>
                    <?php endif; ?>
                    
                    <li>
                        <a href="<?php
                            $idParam = ($resourceType === 'Excavation' ? $itemSetIdForLink : $resource->id());
                            $typeParam = ($resourceType === 'Excavation' ? 'item_set' : 'item');
                            echo $this->url('site/add-triplestore/download-ttl',
                                ['site-slug' => $site->slug()],
                                ['query' => ['id' => $idParam, 'type' => $typeParam]]
                            );
                        ?>" class="action-link" target="_blank">
                            <span class="icon">📥</span> Download TTL Data
                        </a>
                    </li>
                    
                    <li>
                        <a href="<?php echo $this->url('admin/default', ['controller' => ($resourceType === 'Excavation' ? 'item-set' : 'item')], ['query' => ['resource-id' => $resource->id(), 'resource-type' => ($resourceType === 'Excavation' ? 'item_set' : 'item')]]); ?>" class="action-link" target="_blank">
                            <span class="icon">✏️</span> Edit in Admin (Requires Login)
                        </a>
                    </li>
                </ul>
            </div>
            
            <?php if ($resourceType === 'Artifact'): ?>
                <div class="related-excavations">
                    <h3>Related Excavations</h3>
                    <?php $itemSets = $resource->itemSets(); ?>
                    <?php if (count($itemSets) > 0): ?>
                        <ul class="related-list">
                            <?php foreach ($itemSets as $itemSet): ?>
                                <li>
                                    <a href="<?php echo $this->url('site/add-triplestore/view-details', ['site-slug' => $site->slug()], ['query' => ['id' => $itemSet->id(), 'type' => 'item_set']]); ?>">
                                        <?php echo $this->escapeHtml($itemSet->displayTitle()); ?>
                                    </a>
                                </li>
                            <?php endforeach; ?>
                        </ul>
                    <?php else: ?>
                        <p class="no-results">This artifact is not associated with any excavation.</p>
                    <?php endif; ?>
                </div>
            <?php endif; ?>
        </div>
    </div>
    
    <?php if ($resourceType === 'Excavation'): ?>
        <div class="item-set-items">
            <h2>Items in this Excavation</h2>
            
            <?php 
            $arrowheadItems = [];
if (!empty($relatedItems)) {
    foreach ($relatedItems as $item) {
        $title = $item->displayTitle();
        
        // Check if title STARTS WITH "Arrowhead" or "Archaeological Item"
        if (stripos($title, 'Arrowhead') === 0 || stripos($title, 'Archaeological Item') === 0) {
            $arrowheadItems[] = $item;
        }
    }
}



            
            $itemCount = count($arrowheadItems);
            ?>
            
            <?php if ($itemCount > 0): ?>
                <div class="item-count-summary">
                    <p>This excavation contains <?php echo $itemCount; ?> arrowhead<?php echo $itemCount !== 1 ? 's' : ''; ?>.</p>
                </div>
                
                <div class="item-grid">
                    <?php foreach ($arrowheadItems as $item): ?>
                        <div class="item-card">
                            <?php if ($item->thumbnail()): ?>
                                <div class="thumbnail">
                                    <img src="<?php echo $this->escapeHtml($item->thumbnail()->assetUrl()); ?>" alt="<?php echo $this->escapeHtml($item->displayTitle()); ?>">
                            </div>
                            <?php endif; ?>
                            
                            <h3><?php echo $this->escapeHtml($item->displayTitle()); ?></h3>
                            
                            <?php if ($item->displayDescription()): ?>
                                <div class="item-description">
                                    <?php echo $this->escapeHtml(substr($item->displayDescription(), 0, 100)); ?>...
                            </div>
                            <?php endif; ?>
                            
                            <div class="item-actions">
                                <a href="<?php echo $this->url('site/add-triplestore/view-details', ['site-slug' => $site->slug()], ['query' => ['id' => $item->id(), 'type' => 'item']]); ?>" class="details-button">View Details</a>
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>
                
                <div class="add-artifacts-action">
                    <a href="<?php echo $this->url('site/add-triplestore/upload', ['site-slug' => $site->slug()], ['query' => ['item_set_id' => $itemSetIdForLink, 'upload_type' => 'arrowhead', 'mode' => 'form']]); ?>" class="add-button">Add New Arrowheads</a>
                </div>
            <?php else: ?>
                <div class="no-items">
                    <p>There are no arrowheads in this excavation yet.</p>
                    <a href="<?php echo $this->url('site/add-triplestore/upload', ['site-slug' => $site->slug()], ['query' => ['item_set_id' => $itemSetIdForLink, 'upload_type' => 'arrowhead', 'mode' => 'form']]); ?>" class="add-button">Add Arrowheads</a>
                </div>
            <?php endif; ?>
        </div>
    <?php endif; ?>
</div>

<style>
.resource-details {
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
}

.breadcrumbs {
    margin-bottom: 20px;
    color: #666;
}

.breadcrumbs a {
    color: #4CAF50;
    text-decoration: none;
}

.breadcrumbs a:hover {
    text-decoration: underline;
}

.resource-header {
    text-align: center;
    margin-bottom: 40px;
    padding: 30px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border-radius: 12px;
}

.resource-header h1 {
    margin: 0 0 10px 0;
    font-size: 2.5rem;
    color: white;
}

.resource-description {
    color: rgba(255, 255, 255, 0.9);
    line-height: 1.6;
    margin-bottom: 0;
    font-size: 1.2rem;
}

.resource-content {
    display: grid;
    grid-template-columns: 3fr 1fr;
    gap: 30px;
    align-items: start;
}

@media (max-width: 768px) {
    .resource-content {
        grid-template-columns: 1fr;
    }
}

.metadata-section {
    margin-bottom: 30px;
    background: white;
    border-radius: 12px;
    padding: 25px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    border: 1px solid #e0e0e0;
    color: grey ;
}

.metadata-section h2 {
    margin-top: 0;
    margin-bottom: 20px;
    color: #333;
    font-size: 1.5rem;
}

.metadata-table {
    width: 100%;
    border-collapse: collapse;
}

.metadata-table th, .metadata-table td {
    padding: 12px;
    border-bottom: 1px solid #eee;
    text-align: left;
    vertical-align: top;
}

.metadata-table th {
    width: 30%;
    color: #555;
    font-weight: 600;
}

.value {
    margin-bottom: 5px;
}

.related-items {
    margin-top: 30px;
    background: white;
    border-radius: 12px;
    padding: 25px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    border: 1px solid #e0e0e0;
}

.related-items h2 {
    margin-top: 0;
    margin-bottom: 20px;
    color: #333;
    font-size: 1.5rem;
}

.excavation-details, .artifact-details {
    background: white;
    border-radius: 12px;
    padding: 25px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    border: 1px solid #e0e0e0;
    margin-bottom: 30px;
}

.artifact-media {
    margin-bottom: 20px;
}

.media-carousel {
    position: relative;
    max-width: 420px;
    margin: 0 auto 20px auto;
    background: #fafafa;
    border-radius: 10px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.07);
    overflow: hidden;
    text-align: center;
}

.media-carousel .carousel-slide {
    display: none;
    padding: 10px 0 0 0;
}

.media-carousel .carousel-slide img {
    width: 100%;
    max-height: 320px;
    object-fit: contain;
    border-radius: 8px;
    background: #fff;
}

.media-carousel .media-caption {
    font-size: 14px;
    color: #555;
    padding: 10px 10px 16px;
    background: #f5f5f5;
    border-radius: 0 0 8px 8px;
}

.carousel-nav {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(255,255,255,0.8);
    border: none;
    font-size: 2rem;
    color: #333;
    padding: 6px 14px;
    border-radius: 50%;
    cursor: pointer;
    z-index: 2;
    transition: background 0.2s;
}

.carousel-nav:hover {
    background: #e0e0e0;
}

.carousel-nav.prev { left: 10px; }
.carousel-nav.next { right: 10px; }
.carousel-indicators {
    text-align: center;
    margin: 10px 0 12px 0;
}
.carousel-indicators .carousel-dot {
    display: inline-block;
    width: 11px;
    height: 11px;
    margin: 0 4px;
    background: #bbb;
    border-radius: 50%;
    cursor: pointer;
    transition: background 0.2s;
}
.carousel-indicators .carousel-dot.active {
    background: #2196F3;
}
@media (max-width: 600px) {
    .media-carousel { max-width: 98vw; }
    .media-carousel .carousel-slide img { max-height: 180px; }
}

.artifact-context {
    background-color: #f5f5f5;
    padding: 20px;
    border-radius: 8px;
    margin-bottom: 25px;
}

.artifact-context h3 {
    margin-top: 0;
    color: #333;
    font-size: 1.3rem;
}

.context-info {
    margin-top: 15px;
}

.context-property {
    margin-bottom: 12px;
}

.context-property .label {
    font-weight: bold;
    color: #555;
}

.excavation-info {
    margin-bottom: 15px;
}

.excavation-info .label {
    font-weight: bold;
    color: #555;
}

.actions-panel {
    background: white;
    border-radius: 12px;
    padding: 25px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    border: 1px solid #e0e0e0;
    margin-bottom: 25px;
}

.actions-panel h3 {
    margin-top: 0;
    color: #333;
    font-size: 1.3rem;
    margin-bottom: 15px;
}

.action-list {
    list-style-type: none;
    padding-left: 0;
    margin: 0;
}

.action-list li {
    margin-bottom: 12px;
}

.action-link {
    display: flex;
    align-items: center;
    text-decoration: none;
    color: #2196F3;
    padding: 10px;
    border-radius: 8px;
    transition: background-color 0.2s;
}

.action-link:hover {
    background-color: #e3f2fd;
}

.action-link .icon {
    margin-right: 12px;
    font-size: 22px;
}

.related-excavations {
    background: white;
    border-radius: 12px;
    padding: 25px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    border: 1px solid #e0e0e0;
}

.related-excavations h3 {
    margin-top: 0;
    color: #333;
    font-size: 1.3rem;
    margin-bottom: 15px;
}

.related-list {
    list-style-type: none;
    padding-left: 0;
    margin: 0;
}

.related-list li {
    margin-bottom: 10px;
}

.related-list a {
    color: #2196F3;
    text-decoration: none;
    display: block;
    padding: 8px;
    border-radius: 6px;
    transition: background-color 0.2s;
}

.related-list a:hover {
    background-color: #e3f2fd;
    text-decoration: none;
}

.result-cards {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 20px;
    margin-top: 20px;
}

.result-card {
    border-radius: 12px;
    padding: 20px;
    background-color: white;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    border: 1px solid #e0e0e0;
    display: flex;
    flex-direction: column;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.result-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 25px rgba(0,0,0,0.15);
}

.result-card h4 {
    margin-top: 10px;
    margin-bottom: 10px;
    font-size: 18px;
    color: #333;
}

.thumbnail {
    width: 100%;
    height: 140px;
    overflow: hidden;
    margin-bottom: 15px;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: #f5f5f5;
    border-radius: 8px;
}

.thumbnail img {
    max-width: 100%;
    max-height: 100%;
    object-fit: contain;
}

.description {
    font-size: 14px;
    color: #666;
    margin-bottom: 15px;
    line-height: 1.5;
}

.actions {
    margin-top: auto;
    padding-top: 15px;
}

.details-button, .add-button {
    display: block;
    width: 100%;
    padding: 10px 0;
    border-radius: 6px;
    text-align: center;
    text-decoration: none;
    font-size: 14px;
    font-weight: 500;
    transition: background-color 0.2s ease;
}

.details-button {
    background-color: #2196F3;
    color: white;
}

.add-button {
    background-color: #4CAF50;
    color: white;
}

.details-button:hover {
    background-color: #1976D2;
    color: white;
}

.add-button:hover {
    background-color: #45a049;
    color: white;
}

.no-related-items {
    text-align: center;
    padding: 30px;
    background-color: #f9f9f9;
    border-radius: 10px;
    margin-top: 20px;
}

.no-related-items .add-button {
    display: inline-block;
    margin-top: 15px;
    padding: 10px 20px;
}

.item-set-items {
    margin-top: 40px;
    padding-top: 30px;
    border-top: 1px solid #e0e0e0;
}

.item-set-items h2 {
    font-size: 1.8rem;
    color: #333;
    margin-bottom: 20px;
}

.item-count-summary {
    margin-bottom: 25px;
    font-size: 16px;
    color: #555;
    background: white;
    border-radius: 12px;
    padding: 20px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    border: 1px solid #e0e0e0;
}

.item-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 20px;
    margin-bottom: 30px;
}

.item-card {
    background: white;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    border: 1px solid #e0e0e0;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.item-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 25px rgba(0,0,0,0.15);
}

.item-card .thumbnail {
    height: 180px;
    overflow: hidden;
    border-radius: 0;
}

.item-card .thumbnail img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.item-card h3 {
    padding: 15px 15px 10px;
    margin: 0;
    font-size: 18px;
    font-weight: 600;
    color: #333;
}

.item-description {
    padding: 0 15px 15px;
    font-size: 14px;
    color: #666;
    line-height: 1.5;
}

.item-actions {
    padding: 0 15px 15px;
}

.add-artifacts-action {
    margin-top: 25px;
    text-align: center;
}

.add-button, .details-button {
    display: inline-block;
    padding: 12px 25px;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 500;
    text-align: center;
    transition: background-color 0.2s ease;
}

.no-items {
    text-align: center;
    padding: 50px;
    background: white;
    border-radius: 12px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    border: 1px solid #e0e0e0;
}

.no-items p {
    margin-bottom: 25px;
    font-size: 16px;
    color: #666;
}

@media (max-width: 768px) {
    .item-grid {
        grid-template-columns: 1fr;
    }
    
    .resource-header {
        padding: 20px;
    }
    
    .resource-header h1 {
        font-size: 2rem;
    }
    
    .resource-description {
        font-size: 1rem;
    }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const links = document.querySelectorAll('.lightbox-link');
    links.forEach(link => {
        link.addEventListener('click', function(e) {
            e.preventDefault();
            const imgSrc = this.href;
            const title = this.getAttribute('data-title') || '';
            let lightbox = document.getElementById('lightbox-modal');
            if (!lightbox) {
                lightbox = document.createElement('div');
                lightbox.id = 'lightbox-modal';
                lightbox.innerHTML = `
                    <div class="lightbox-overlay"></div>
                    <div class="lightbox-content">
                        <img src="" alt="" id="lightbox-img">
                        <div class="lightbox-caption" id="lightbox-caption"></div>
                        <button class="lightbox-close">&times;</button>
                    </div>
                `;
                document.body.appendChild(lightbox);
                lightbox.querySelector('.lightbox-overlay').onclick = closeLightbox;
                lightbox.querySelector('.lightbox-close').onclick = closeLightbox;
            }
            lightbox.style.display = 'flex';
            document.getElementById('lightbox-img').src = imgSrc;
            document.getElementById('lightbox-caption').textContent = title;
            function closeLightbox() {
                lightbox.style.display = 'none';
            }
        });
    });
});
</script>

<script>
document.addEventListener('DOMContentLoaded', function() {
    let slideIndex = 0;
    const slides = document.querySelectorAll('#media-carousel .carousel-slide');
    const dots = document.querySelectorAll('#media-carousel .carousel-dot');
    const prev = document.getElementById('carousel-prev');
    const next = document.getElementById('carousel-next');
    let startX = null;

    function showSlide(n) {
        if (!slides.length) return;
        slideIndex = (n + slides.length) % slides.length;
        slides.forEach((slide, i) => {
            slide.style.display = i === slideIndex ? 'block' : 'none';
        });
        dots.forEach((dot, i) => {
            dot.classList.toggle('active', i === slideIndex);
        });
    }

    if (prev && next) {
        prev.onclick = () => showSlide(slideIndex - 1);
        next.onclick = () => showSlide(slideIndex + 1);
    }
    dots.forEach(dot => {
        dot.onclick = () => showSlide(Number(dot.dataset.index));
    });

    const carousel = document.getElementById('media-carousel');
    if (carousel) {
        carousel.addEventListener('touchstart', function(e) {
            if (e.touches.length === 1) startX = e.touches[0].clientX;
        });
        carousel.addEventListener('touchend', function(e) {
            if (startX === null) return;
            let endX = e.changedTouches[0].clientX;
            if (endX - startX > 40) showSlide(slideIndex - 1);
            else if (startX - endX > 40) showSlide(slideIndex + 1);
            startX = null;
        });
    }

    showSlide(0);
});
</script>